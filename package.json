{
  "name": "browserlobby",
  "version": "1.0.0",
  "description": "",
  "main": "dist/index.js",
  "scripts": {
    "clean": "rimraf dist/",
    "copy-files": "cp -r src/public dist/ && cp -r src/views dist/",
    "prebuild": "tslint -c tslint.json -p tsconfig.json --fix",
    "build": "yarn clean && tsc && yarn copy-files",
    "start": "node .",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/Zwetsche/BrowserLobby.git"
  },
  "author": "",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/Zwetsche/BrowserLobby/issues"
  },
  "homepage": "https://github.com/Zwetsche/BrowserLobby#readme",
  "private": true,
  "devDependencies": {
    "@types/express": "^4.17.11",
    "@types/node": "^15.0.1",
    "copyfiles": "^2.4.1",
    "path": "^0.12.7",
    "pm2": "^4.5.6",
    "rimraf": "^3.0.2",
    "tslint": "^6.1.3",
    "typescript": "^4.2.4",
    "yarn": "^1.22.10"
  },
  "dependencies": {
    "express": "^4.17.1"
  }
}
